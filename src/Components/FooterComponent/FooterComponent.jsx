import React from "react";

const FooterComponent = ({label}) => {
    return(
        <div style={{width : '100%', textAlign : 'center', margin : '100px 0px'}}>
            {label}
        </div>
    )
}

export default FooterComponent;